package com.demo.arraystest;

public enum Operations {
	accept, display, add_end, add_pos, search_value, delete_pos, delete_value, rotate_array, reverse_array, find_max,
	exchange_index_value, find_sum, Exit
}
